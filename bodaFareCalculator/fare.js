function calculateBodaFare(){
    let user = prompt("Enter distance of trips in kilometers");
     distance = number(Kilometers);
     const BaseFare = 50;
     const perKm = 15;
     const totalFare = baseFare + (distanceInKm * chargePerKm);


     console.log(`Distance: ${distance} in Km`);
     console.log(`Fare: ${fare in shillings}`);
     console.log(`Unafika wapi mkubwa`);
     console.log(`Ukikalia Pikipiki: ${baseFare}`);
     console.log(`Mpaka huko: ${d}istanceInKm * chargePerKm`);
     console.log(`Total: ${totalFare}`);


}